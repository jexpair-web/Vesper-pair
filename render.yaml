services:
  - type: web
    name: junex-ultra-session
    runtime: node
    plan: free
    buildCommand: npm install --legacy-peer-deps
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
